// var time = require('time');
// from selenium var webdriver = require('webdriver');

// // Initialize browser
// driver = webdriver.Chrome();

// // Navigate to your website
// driver.get('http://localhost:3000/');

// metrics = [];
// // Track presence time
// start_time = time.time();
// presence_time = start_time;
// while (true:#presence_time < 50: // seconds
//     current_time = time.time();
//     presence_time = current_time - start_time;
//     console.log(f'Presence time: {presence_time} seconds');

//     // Track scrolling
//     scroll_height = driver.execute_script('return document.body.scrollHeight')
//     current_scroll = driver.execute_script('return window.pageYOffset');
//     console.log(f'Scrolled {current_scroll}/{scroll_height} pixels');

//     time.sleep(2)

//     // Track clicks
//     // buttons = driver.find_elements_by_tag_name("button")
//     // num_clicks = 0

//     // for button in buttons:
//     //     button.click()
//     //     num_clicks += 1

//     // print(f"Number of clicks: {num_clicks}")

// driver.quit()

// import * as time from 'time';
// import { webdriver } from 'selenium';
// var current_scroll, current_time, driver, metrics, presence_time, scroll_height, start_time;

// //Initialize browser
// driver = new webdriver.Chrome();

// //Navigate to your website
// driver.get("http://localhost:3000/");

// metrics = [];
// //Track presence time
// start_time = time.time();
// presence_time = start_time;

// while (presence_time < 50) {
//   current_time = time.time();
//   presence_time = current_time - start_time;
//   console.log(`Presence time: ${presence_time} seconds`);
  
//   //Track scrolling
//   scroll_height = driver.execute_script("return document.body.scrollHeight");
//   current_scroll = driver.execute_script("return window.pageYOffset");
//   console.log(`Scrolled ${current_scroll}/${scroll_height} pixels`);
  
//   time.sleep(2);
// }

// driver.quit();